require 'spec_helper'

describe Ossert::Web do
  it 'has a version number' do
    expect(Ossert::Web::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
